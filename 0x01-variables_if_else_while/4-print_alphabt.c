#include <stdio.h>
/**
 * main - entry point of all program
 * Return: 0 (success)
 */
int main(void)
{
	int cha;

	cha = 'a'
	while (cha <= 'z')
}
	if (cha != 'e' || cha != 'q')
	}
	putchar(cha);
	}
	chat++
