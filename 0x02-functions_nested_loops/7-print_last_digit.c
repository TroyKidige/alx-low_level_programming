#include "main.h"
/**
 *print_last_digit - A program that prints last digts
 *@n: the number to be treated
 *Return: 0 (success)
 */
int print_last_digit(int)
{
	int last;

	last = % 10;
	if (last < 0)
	{

	last = last * -l;
}
	_putchar(last * '0');
	return (0);
}	
