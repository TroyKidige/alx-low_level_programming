#include "main.h"
/**
 *_abs - a program that prints integers
 *@n: a function parameter that prints
 *Return: 0 (success)
 */
int _abs(int)
{
	if (a < 0)
		return (-a);
	else (a >= 0)
{
		return (a);
		}
}
