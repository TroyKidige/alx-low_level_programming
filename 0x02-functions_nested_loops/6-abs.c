#include "main.h"
/**
 * _abs - a program that prints integers
 * @n: a function parameter that prints
 *Return: 0 (success)
 */
int _abs(int)
	int a;
{
	if (a < 0)
		return (-a);
	else if (a >= 0)
{
		return (a);
		}
}
